server:
  port: 80


eureka:
  client:
    service-url:
      #单机版
      defaultZone: http://eureka7001.com:7001/eureka
    register-with-eureka: true
    fetch-registry: true

spring:
  application:
    name: cloud-orderfeign-service